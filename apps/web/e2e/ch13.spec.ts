import { expect, test } from '@playwright/test';
// test-id: playwright:ch13-factual-purchase-list
async function login(page: import('@playwright/test').Page) { await page.goto('/login?next=%2Fpurchases'); await page.getByLabel('Usuario o correo').fill('admin@demo.local'); await page.getByLabel('Clave').fill('demo-admin'); await page.getByRole('button', { name: 'Iniciar sesión' }).click(); await expect(page).toHaveURL(/\/purchases$/); }
test('CH13 renders a factual purchase list without financial operations', async ({ page }) => { await login(page); const before = await page.evaluate(() => localStorage.getItem('analiza.en.casa.workspace.v3.auditEntries')); await expect(page.getByRole('heading', { name: 'Listado' })).toBeVisible(); for (const header of ['Acciones', 'Tipo', 'Número', 'Proveedor', 'Total', '# Factura', 'Fecha', 'Estado', 'Registro PT']) await expect(page.getByRole('columnheader', { name: header })).toBeVisible(); await expect(page.getByRole('button', { name: 'Excel' })).toBeDisabled(); await page.getByLabel('Buscar compras').fill('sin-compra-ch13'); await expect(page.locator('tbody .empty-state')).toContainText('Sin compras documentadas'); await expect(page.locator('tbody .empty-state')).toContainText('sin-compra-ch13'); await page.reload(); await expect.poll(() => page.evaluate(() => localStorage.getItem('analiza.en.casa.workspace.v3.auditEntries'))).toBe(before); });
test('CH13 permits AUDITOR read-only list access and denies NURSE directly', async ({ browser }) => { const auditor = await browser.newPage(); await auditor.goto('/login?next=%2Fpurchases'); await auditor.getByLabel('Usuario o correo').fill('auditor@demo.local'); await auditor.getByLabel('Clave').fill('demo-auditor'); await auditor.getByRole('button', { name: 'Iniciar sesión' }).click(); await expect(auditor.getByRole('heading', { name: 'Listado' })).toBeVisible(); await expect(auditor.getByRole('button', { name: 'Nueva compra' })).toHaveCount(0); const nurse = await browser.newPage(); await nurse.goto('/login?next=%2Fpurchases'); await nurse.getByLabel('Usuario o correo').fill('nurse@demo.local'); await nurse.getByLabel('Clave').fill('demo-nurse'); await nurse.getByRole('button', { name: 'Iniciar sesión' }).click(); await expect(nurse.locator('main[role="alert"]')).toContainText('Acceso restringido para el rol NURSE'); await auditor.close(); await nurse.close(); });
