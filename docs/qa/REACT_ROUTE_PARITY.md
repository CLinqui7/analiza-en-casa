# React route parity

Machine-readable source: `docs/qa/REACT_ROUTE_PARITY.json`.

The initial manifest deliberately records every currently resolvable migration gap. `npm run react:parity` is expected to fail until every legacy route is migrated exactly or has a documented client blocker.
