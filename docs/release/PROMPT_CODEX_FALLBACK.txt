# ANALIZA EN CASA · RECUPERACIÓN AUTOMÁTICA DE UI POLISH

Trabaja SOLO en el worktree indicado por el launcher y en la rama `codex/ui-polish-final-20260904`.

Contexto obligatorio:
- Base certificada: `origin/codex/reconcile-global-release-20260903`.
- NO tocar `main`.
- NO `reset --hard`, NO `git clean`, NO force-push.
- NO modificar evidencia inmutable bajo `references/video-audit/`.
- Mantener el logo oficial en `apps/web/public/brand/analiza-en-casa-logo.png`.
- Mantener el rediseño del shell: sidebar fijo dentro del viewport, `nav-scroll` independiente, `.main-content` con scroll independiente, persistencia de `analiza.sidebar.scrollTop`, y `scroll={false}` en navegación lateral.
- Mantener todos los `data-action-id`, permisos y restricciones actuales.
- `vercel.json` NO debe tener `crons` en esta rama de staging. Conservar `api/cron-retries.js`.
- La base de datos se gestionará separadamente en Supabase; no inventes credenciales ni secretos.
- Lee `docs/release/FINAL_VIDEO_FUNCTION_AUDIT.md` y el JSON canónico `docs/qa/VIDEO_TO_REACT_TRACEABILITY.json`.
- 210/210 trazados NO significa 210/210 implementados.
- No inventar reglas clínicas, financieras, escalas o integraciones. Si una función está bloqueada por definición de cliente, aprobación clínica, integración o conflicto de fuente, mantenerla bloqueada y documentada.
- Los MISSING/PARTIAL sin blocker explícito sí deben investigarse y corregirse si la evidencia soporta el comportamiento.

Tu trabajo:
1. Lee `docs/release/UI_POLISH_FAILURE.log` y `docs/release/UI_POLISH_STATE.json`.
2. Reproduce el gate que falló.
3. Clasifica APP BUG vs TEST STALE vs ENVIRONMENT.
4. Corrige la causa mínima sin debilitar pruebas.
5. Para formato, no incluyas archivos `.txt` como `PROMPT_CODEX_FALLBACK.txt` en Prettier: no tienen parser soportado. Valida esos documentos de texto con `git diff --check`; conserva `npm run format:check` para los formatos que Prettier sÃ­ admite.
6. Reejecuta el gate fallido y luego los gates posteriores.
6. Ejecuta como mínimo:
   - npm run format:check
   - npm run lint
   - npm run typecheck
   - npm run test:react
   - npm run qa:light-mode
   - npm run qa:client-changes
   - npm run qa:video-parity
   - npm run qa:traceability-mirror
   - npm run audit:verify
   - npm run security:scan
   - npm run build
   - npx playwright test
   - npm run test:browser:react
   - Selenium global contra un servidor sano del MISMO worktree
   - npm run selenium:coverage
   - npm run test:selenium
7. Si todo pasa, stagea SOLO archivos intencionales, `git diff --cached --check`, commit y push de `codex/ui-polish-final-20260904`.
8. Actualiza `docs/release/UI_POLISH_STATE.json` con estado, HEAD, gates y siguiente acción.
9. No declares terminado si un gate ejecutable sigue rojo.
