# CH17 F04 evidence and data-boundary precheck

Opened `CH17-E0012`, `CH17-E0017`, and `CH17-E0019`. The first frame establishes only a double-calendar presentation with `Seleccionar` and `Cancelar`; its dates are examples, not an approved range policy. The next frame shows `Procesando`, and the result exposes sensitive patient and hospitalization fields.

`CH17-F04` is therefore `MISSING`, blocked by `CH16-Q008` and the new range-specific `CH17-Q008`. No calendar, report query, patient selector, printing/export, navigation, or clinical write has been added.
